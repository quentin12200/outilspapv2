# Gestion des branches

## Branche par défaut
- `codex/fix-database-connection-issues-on-railway-j7d0jj` — référence principale du projet sur Railway.

## Sauvegardes locales (5 dernières)
Les cinq instantanés les plus récents sont conservés sur des branches dédiées pour faciliter le retour arrière :

1. `sauvegarde-invitations-pap-c5` — instantané du tableau de bord PAP C5 (commit 5a90292).
2. `sauvegarde-correction-tous-pv` — correction du lien avec la table `Tous_PV` (commit 91510c6).
3. `sauvegarde-alignement-colonnes` — alignement des colonnes SQLite/Python (commit 444a9d1).
4. `sauvegarde-schema-v1` — migrations complètes du schéma v1.0.0 (commit 2e3d442).
5. `sauvegarde-telechargement-bdd` — configuration du téléchargement automatique de la base (commit 425a343).

## Archives
Les références plus anciennes ou expérimentales sont listées dans `ARCHIVES_BRANCHES.md`.
